# Movable Panels Widget

Provides the `MovablePanels` controller that initialises Split.js layouts for the three-column workspace (left, middle, right) with vertical splits inside each column, exposing resize hooks for dependent widgets.
